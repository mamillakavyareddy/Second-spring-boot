package in.co.kavya.SecondSpringBoot;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class SecondSpringBootApplication {

	public static void main(String[] args) {
		SpringApplication.run(SecondSpringBootApplication.class, args);
	}

}
	//create one mvc controller with name student controller
	//create student table in my sql data base
	//create a view (student.html)to display  the student in a tabular format
	//add bootstrap css in the table component of view